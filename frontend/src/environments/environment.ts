export const environment = {
  production: true,
  // Swapped for the API Gateway URL once that's built; direct service URLs for now.
  authApiUrl: '/api/v1',
  courseApiUrl: '/api/v1',
  enrollmentApiUrl: '/api/v1',
  quizApiUrl: '/api/v1',
};
